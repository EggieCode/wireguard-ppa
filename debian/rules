#!/usr/bin/make -f

include /usr/share/dpkg/default.mk

export DEB_BUILD_MAINT_OPTIONS = hardening=+all

%:
	dh $@ --with bash-completion --with dkms

override_dh_auto_install-indep:
	$(MAKE) -C wireguard-src/src DESTDIR=../../debian/wireguard-dkms DKMSDIR=/usr/src/wireguard-$(DEB_VERSION_UPSTREAM)/ dkms-install

override_dh_auto_build-arch:
	$(MAKE) -C wireguard-src/src/tools

override_dh_dkms:
	dh_dkms -p wireguard-dkms -- wireguard-src/src/dkms.conf
