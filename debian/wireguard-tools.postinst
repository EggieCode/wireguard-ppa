#!/bin/sh
# postinst script for WireGuard

#DEBHELPER#

case $1 in
    configure)
        # Symlinks
	#dpkg-divert --add --package wireguard-tools --rename \
	#	--divert /usr/bin/wg-quick /usr/bin/wg-quick.bash
        ;;

    abort-upgrade|abort-remove|abort-deconfigure)
        ;;

    *)
        echo "postinst called with unknown argument \`$1'" >&2
        ;;

esac

exit 0
